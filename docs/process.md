# Timer App — Progress

## Проект

Многопользовательское веб-приложение для управления таймерами внутри команды.

Основная идея:

- команда работает с общим списком таймеров;
- таймеры можно запускать, останавливать, перезапускать, завершать локально;
- часть действий доступна только пользователям с определённой ролью;
- в будущем данные будут храниться в БД и синхронизироваться между пользователями.

Стек:

- Frontend: Vue 3 + Vite + TypeScript + Pinia
- Backend: планируется Python + FastAPI + Supabase
- Хостинг: GitHub Pages + бесплатные сервисы

Подробная архитектура, модели данных, роли, Pinia store и будущая backend/Supabase-структура описаны в [`architecture.md`](./architecture.md).

---

## Что уже реализовано

### Общая структура

- Создан репозиторий.
- Настроен Vue-проект.
- Добавлена структура проекта:
  - `components`
  - `pages`
  - `stores`
  - `utils`
  - `composables`
  - `types`

### Основная frontend-логика

Реализовано:

- таблица таймеров;
- realtime countdown;
- расчёт оставшегося времени;
- поддержка `timeShiftSeconds`;
- поддержка остановленных таймеров;
- поддержка локального завершения таймера;
- поддержка локального отключения звука у конкретного таймера;
- поддержка общего отключения звука для всех таймеров;
- поддержка звукового сигнала при переходе в `signal`;
- добавление таймера;
- редактирование таймера;
- удаление изображения таймера в режиме редактирования;
- удаление таймера;
- перезапуск таймера;
- перезапуск таймера со сдвигом;
- обновление даты, времени, смещения и пользователя последнего запуска при перезапуске таймера;
- остановка таймера;
- drag-and-drop перестановка таймеров;
- роли `admin`, `manager`, `member` на уровне UI.

---

## Основные компоненты

### `TimersPage.vue`

Главная страница таймеров.

Отвечает за:

- подключение `TimersToolbar`;
- подключение `TimersTable`;
- получение таймеров из Pinia;
- поиск;
- фильтр по статусу;
- сортировку;
- расчёт view-статуса;
- режим перестановки таймеров;
- draft-порядок таймеров при drag-and-drop.

Важная логика:

- обычный режим отображает таймеры с учётом фильтров, поиска и сортировки;
- если пользователь не выбрал сортировку, таймеры отображаются по `sortOrder`;
- в режиме перестановки фильтры, поиск и сортировка сбрасываются;
- в режиме перестановки отображается полный список таймеров по `sortOrder`.

---

### `TimersToolbar.vue`

Панель над таблицей.

Содержит:

- фильтр “Все / Активные”;
- поиск;
- общий переключатель звука;
- кнопку “Поменять местами” / “Сохранить порядок”;
- кнопку “Добавить таймер”.

Реализовано:

- кнопка добавления открывает `AddTimerModal`;
- кнопка перестановки включает/выключает reorder mode;
- в reorder mode поиск и фильтры disabled;
- кнопка добавления disabled в reorder mode;
- общий переключатель звука включает/выключает звук для всех таймеров;
- переключатель использует статичные иконки выключенного и включённого звука по бокам от тумблера;
- права доступа учитываются через `usePermissions`.

---

### `TimersTable.vue`

Таблица таймеров.

Содержит:

- заголовки колонок;
- сортировку по названию;
- сортировку по оставшемуся времени;
- список `TimerRow`;
- empty state;
- drag-and-drop режим перестановки.

Реализовано:

- в обычном режиме таблица просто отображает переданный список;
- в reorder mode строки становятся draggable;
- при drop таблица отдаёт новый порядок наверх через emit `reorder`;
- сортировка disabled в reorder mode.

---

### `TimerRow.vue`

Строка таймера.

Отвечает за:

- отображение названия;
- изображение или placeholder;
- статус;
- описание;
- длительность;
- оставшееся время;
- дату, время, смещение и пользователя последнего запуска;
- кнопки действий.

Действия:

- перезапустить;
- остановить;
- включить/выключить звук для конкретного таймера;
- редактировать;
- удалить;
- завершить, если таймер в `signal`.

Реализовано:

- открытие `RestartTimerModal`;
- открытие `AddTimerModal` в режиме редактирования;
- открытие `ConfirmDeleteModal`;
- disabled-состояния кнопок по ролям;
- disabled-состояния кнопок в reorder mode;
- информация о последнем запуске берётся из данных таймера, а не из статичных значений;
- после перезапуска таймера в строке обновляются дата, время, смещение и пользователь последнего запуска;
- звук таймера проигрывается только если включён общий звук и включён звук конкретного таймера;
- при выключении общего звука проигрываемый сигнал останавливается;
- остановка звука при действиях.

---

### `RestartTimerModal.vue`

Модалка повторного запуска таймера.

Режимы:

- запуск от текущего времени;
- запуск со сдвигом.

Реализовано:

- валидация целых минут;
- дефолтное смещение: `1` минута;
- смещение должно быть не меньше `1` минуты;
- смещение не может превышать базовую длительность таймера;
- кнопка запуска disabled при ошибке;
- аккуратная подсветка ошибки;
- фокус и disabled-состояния приведены к стилю приложения.

---

### `AddTimerModal.vue`

Универсальная модалка для создания и редактирования таймера.

Режимы:

- `create`
- `edit`

В режиме создания:

- поля пустые;
- после сохранения создаётся новый мок-таймер в Pinia.

В режиме редактирования:

- поля заполнены данными выбранного таймера;
- можно изменить:
  - название;
  - описание;
  - длительность;
  - изображение;
- можно удалить текущее изображение;
- после сохранения изменения применяются в Pinia.

Валидация:

- название обязательно;
- длительность обязательна;
- длительность должна быть целым числом минут;
- длительность должна быть больше `0`.

Изображение:

- если у таймера уже есть изображение, в режиме редактирования показывается кнопка удаления изображения;
- кнопка удаления использует тот же `IconButton` и `deleteIcon`, что и кнопка удаления таймера в `TimerRow.vue`;
- кнопка удаления расположена по правому краю строки выбора файла;
- удаление изображения не применяется сразу, а только помечается через `removeImage`;
- если пользователь нажмёт “Отмена”, изображение останется без изменений;
- если пользователь нажмёт “Сохранить”, `updateTimer` очистит `imageUrl`;
- если после удаления пользователь выберет новый файл, удаление отменяется и сохраняется новый файл.

Важно:

- выбранное изображение сейчас превращается в `URL.createObjectURL(file)`;
- это временное решение для фронта;
- после перезагрузки страницы такая картинка не сохранится;
- в будущем изображение нужно будет загружать в Supabase Storage или другой storage.

---

### `ConfirmDeleteModal.vue`

Модалка подтверждения удаления.

Сценарий:

- пользователь нажимает “Удалить”;
- открывается подтверждение;
- “Отмена” закрывает модалку;
- “Удалить” удаляет таймер из Pinia;
- строка пропадает из таблицы;
- локальный completed для таймера очищается.

---

## Pinia store

Файл:

```txt
src/stores/timers.ts
```

Сейчас store содержит мок-данные и actions.

Реализованные actions:

- `createTimer(payload)`
- `updateTimer(timerId, payload)`
- `deleteTimer(timerId)`
- `restartTimer(timerId, timeShiftSeconds)`
- `stopTimer(timerId)`
- `reorderTimers(timerIds)`

Кратко:

- `createTimer` создаёт новый таймер локально;
- `updateTimer` редактирует существующий таймер локально;
- `deleteTimer` удаляет таймер и очищает локальный completed;
- `restartTimer` перезапускает таймер, обновляет данные последнего запуска и сбрасывает локальный completed;
- `stopTimer` останавливает таймер;
- `reorderTimers` сохраняет новый порядок через `sortOrder`.

Подробности описаны в [`docs/architecture.md`](./docs/architecture.md).

---

## Роли и права

Текущие роли:

```ts
type UserRole = 'admin' | 'manager' | 'member'
```

Сейчас роль моковая в `src/composables/usePermissions.ts`.

Для проверки можно менять вручную:

```ts
const currentRole = ref<UserRole>('admin')
const currentRole = ref<UserRole>('manager')
const currentRole = ref<UserRole>('member')
```

Кратко:

- `admin` может выполнять все действия с таймерами и менять порядок;
- `manager` может создавать, редактировать, удалять, запускать и останавливать таймеры, но не может менять порядок;
- `member` может запускать и останавливать таймеры;
- звук, поиск, фильтры и сортировка считаются локальными действиями пользователя.

Важно:

- текущие права являются только UI-ограничением;
- настоящую безопасность нужно будет реализовать на backend/Supabase RLS.

---

## Перестановка таймеров

Реализовано.

Идея:

- админ может менять глобальный порядок таймеров;
- порядок хранится в поле `sortOrder`;
- в будущем в БД это будет поле `sort_order`.

Сценарий:

1. Admin нажимает “Поменять местами”.
2. Таблица переходит в reorder mode.
3. Кнопка превращается в “Сохранить порядок”.
4. Поиск, фильтры, сортировка и действия в строках становятся disabled.
5. Строки можно перетаскивать drag-and-drop.
6. После повторного нажатия “Сохранить порядок” новый порядок сохраняется в Pinia.
7. У таймеров обновляется `sortOrder`.

Важные детали:

- Drag-and-drop сделан без сторонних библиотек.
- Во время reorder mode используется `reorderDraftTimers`.
- Новый порядок применяется только после сохранения.
- При входе в reorder mode:
  - поиск очищается;
  - фильтр сбрасывается на `all`;
  - сортировка отключается;
  - таймеры показываются по `sortOrder`.

---

## Поиск, фильтры и сортировка

Поиск ищет по:

- `title`;
- `description`.

Фильтры:

- `all`;
- `active`.

Фильтр `active` показывает:

- `active`;
- `signal`.

Сортировка:

- по названию;
- по оставшемуся времени.

Если сортировка не выбрана:

- таймеры отображаются по `sortOrder`.

Сортировка является локальной для пользователя и не меняет `sortOrder`.

---

## Временные frontend-only решения

Эти части сейчас сделаны для проверки UI и логики без backend.

### Мок-таймеры

Таймеры пока хранятся в Pinia store как мок-массив.

После перезагрузки страницы:

- добавленные таймеры исчезают;
- отредактированные таймеры возвращаются к мок-значениям;
- удалённые таймеры возвращаются;
- изменённый порядок возвращается к мок-значениям.

### Изображения

Выбранные изображения сейчас отображаются через:

```ts
URL.createObjectURL(file)
```

Это временно.

В будущем:

- файл нужно отправлять в Supabase Storage;
- в таймере хранить постоянный `imageUrl`.

### Настройки звука

Настройки звука пока сохраняются в `localStorage`.

Сейчас есть два уровня настроек:

- общий звук для всех таймеров;
- звук конкретного таймера.

Итоговое правило:

```ts
globalSoundEnabled && timerSoundEnabled

### Роли

Роль пользователя сейчас моковая в `usePermissions.ts`.

В будущем:

- роль должна приходить из auth / профиля пользователя;
- ограничения должны проверяться не только на фронте, но и на backend / Supabase RLS.

### События действий

Сейчас действия сразу меняют Pinia.

В будущем:

- `createTimer`;
- `updateTimer`;
- `deleteTimer`;
- `restartTimer`;
- `stopTimer`;
- `reorderTimers`

должны вызывать API / Supabase, а локальное состояние обновляться после успешного ответа или через realtime.

---

## Следующие крупные шаги

### 1. Подготовить слой API/service

Сейчас store напрямую меняет мок-данные.

Позже лучше вынести работу с данными в сервис:

```txt
src/services/timerService.ts
```

Пример будущих методов:

```ts
getTimers()
createTimer(payload)
updateTimer(timerId, payload)
deleteTimer(timerId)
restartTimer(timerId, timeShiftSeconds)
stopTimer(timerId)
reorderTimers(timerIds)
```

На первом этапе сервис может работать с моками, потом его можно заменить на Supabase.

---

### 2. Подключить Supabase

Нужно будет реализовать:

- авторизацию;
- таблицу пользователей;
- таблицу таймеров;
- роли;
- realtime обновления;
- storage для изображений;
- RLS-политики.

---

### 3. Продумать структуру БД

Предполагаемая таблица `timers` описана в [`architecture.md`](./architecture.md).

Важно:

- `sound_enabled`, скорее всего, не должен быть глобальным полем таймера;
- отключение звука сейчас локально для пользователя;
- в будущем это может быть отдельная user settings таблица.

---

### 4. Пользователи и роли

Планируемые роли:

- `admin`
- `manager`
- `member`

Admin:

- все действия с таймерами;
- управление пользователями.

Manager:

- создание;
- редактирование;
- удаление;
- запуск;
- остановка;
- не может менять порядок таймеров.

Member:

- запуск;
- остановка;
- локальные действия вроде звука, поиска, фильтров и сортировки.

---

### 5. Страница управления пользователями

Пока не реализована.

План:

- список пользователей;
- изменение роли;
- приглашение пользователя;
- удаление пользователя из команды.

---

### 6. Логи

Пока не реализованы.

Возможные события:

- таймер создан;
- таймер отредактирован;
- таймер удалён;
- таймер перезапущен;
- таймер остановлен;
- порядок таймеров изменён;
- пользователь изменил роль.

---

## Что проверять после изменений

После любых крупных изменений запускать:

```bash
npm run build
```

Ручная проверка:

```txt
1. Таймеры отображаются.
2. Countdown работает.
3. Signal появляется при истечении таймера после открытия страницы.
4. Completed появляется для таймера, который истёк до открытия страницы.
5. Кнопка “Завершить” работает локально.
6. Перезапуск работает.
7. После перезапуска обновляются дата, время, смещение и пользователь последнего запуска.
8. Перезапуск со сдвигом валидируется.
9. Остановка работает.
10. Индивидуальное отключение звука у таймера работает.
11. Общее отключение звука для всех таймеров работает.
12. Если общий звук выключен, сигнал таймеров не проигрывается.
13. Если общий звук выключить во время сигнала, звук останавливается.
14. После перезагрузки страницы настройки звука сохраняются.
15. Добавление таймера работает.
16. Редактирование таймера работает.
17. Удаление изображения таймера в режиме редактирования работает.
18. Удаление таймера работает.
19. Режим перестановки работает.
20. Роли admin / manager / member корректно меняют доступность кнопок.
```

---

## Контекст для продолжения в новом чате

Можно вставить в новый диалог:

```txt
Продолжаем Timer App.

Это Vue 3 + Vite + TypeScript + Pinia приложение для командного управления таймерами.

Уже реализованы:
- таблица таймеров;
- realtime countdown;
- статусы active / stopped / signal / completed;
- localStorage для completed и sound settings;
- restart modal со сдвигом и валидацией;
- add/edit modal;
- удаление изображения таймера в режиме редактирования;
- delete confirmation modal;
- Pinia actions create/update/delete/restart/stop/reorder;
- роли admin / manager / member через usePermissions;
- UI-disabled кнопок по правам;
- drag-and-drop перестановка таймеров через sortOrder.

Backend ещё не подключён. Сейчас данные хранятся в Pinia как моки. Позже планируется Python + FastAPI + Supabase, auth, роли, storage, realtime и RLS.

Подробная архитектура лежит в docs/architecture.md.

Нужно продолжать с текущей архитектуры и не ломать уже реализованную frontend-логику.
```